// This loading file ensures skeleton shows during route navigation within connect section
export default function Loading() {
  // Return null since each sub-route has its own loading state
  return null
}